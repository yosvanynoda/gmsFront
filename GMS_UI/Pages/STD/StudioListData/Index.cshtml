@page
@model GMS_UI.Pages.STD.StudioListData.IndexModel
@{
    ViewData["Title"] = "StudioList";
}

<div class="container-gms">

    <div class="alert alert-success collapse" studio="alert" id="successAlert">
        <button type="button" class="btn-close float-end" onclick="hideAlerts('successAlert')" aria-label="Close"></button>
        <h4 class="alert-heading" id="successTitle"></h4>
        <p id="successMsg"></p>
    </div>

    <div class="alert alert-danger collapse" studio="alert" id="failedAlert">
        <button type="button" class="btn-close float-end" onclick="hideAlerts('failedAlert')" aria-label="Close"></button>
        <h4 class="alert-heading" id="failedTitle"></h4>
        <p id="failedMsg"></p>
    </div>

    <div class="card shadow border rounded-3 bg-light" style="width:100%; height:90vh">
        <div class="card-header">
            <div class="row">
                <div class="col">
                    <h4>Clinical Studies</h4>
                </div>
                <div class="col">
                    <a class="btn btn-sm btn-success float-end" href="./Create"><i class="bi bi-plus-circle"></i> Add</a>
                </div>
            </div>
        </div>
        <div class="card-body">
            <div id="studioListGrid" style="height:100%; width: 100%;" class="ag-theme-balham"></div>
        </div>
    </div>
</div>

    <script>
        window._csrfToken = $('@Html.AntiForgeryToken()').val();
        urlIndex = '/STD/StudioListData';
    </script>
    <script src="/js/std/studioListData/studioListData.js"></script>

